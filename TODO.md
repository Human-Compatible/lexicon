# TODO for multi-app deployment from one repo

- [ ] Make favicon and logo assets within `frontend/assets` and
      `frontend/images` be per-app configurable
- [ ] Make app name within package.json per-app configurable
